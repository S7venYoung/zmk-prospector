# Retro Workstation

Exact 280x240 RGB565 pixel-art receiver theme for Prospector. The artwork is
clipped to the display's rounded-glass safe area and adds no runtime framebuffer.

Select it on the receiver only with `prospector_theme_retro_workstation`.
