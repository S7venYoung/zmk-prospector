# Weather Clock

Weather, date, time, WPM, split-battery and macOS modifier dashboard for the
rotated 280x240 Prospector display. The host-status service supplies clock and
weather fields over the existing Studio custom RPC; keyboard status stays local.

Select with `prospector_theme_weather_clock` on a receiver-only build.
